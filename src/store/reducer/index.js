// import types from '../constants';

const initialState = {
    profile: {},
};

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return {
                ...state,
            }
        }
    }
};

export default reducer;